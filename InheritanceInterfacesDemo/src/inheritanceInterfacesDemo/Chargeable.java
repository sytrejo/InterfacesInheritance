package inheritanceInterfacesDemo;

//to denote an interface use the keyword interface
public interface Chargeable {
	
	//having no method body makes this an instance. (?)
	void charge(double amps);

}
